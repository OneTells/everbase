[project]
name = "everbase"
version = "2.5.0"

requires-python = ">=3.12,<4.0"

dependencies = [
    "asyncpg~=0.30",
    "pydantic~=2.11",
    "SQLAlchemy~=2.0"
]

[project.optional-dependencies]
fast = [
    "orjson~=3.10",
]
